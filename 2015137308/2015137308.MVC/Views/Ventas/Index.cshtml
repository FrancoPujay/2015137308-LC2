@model IEnumerable<_2015137308.Entities.Entities.Venta>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Administrativo.Dni)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cliente.Dni)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Servicio.ServicioId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TipoComprobante)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TipoPago)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Administrativo.Dni)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cliente.Dni)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Servicio.ServicioId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TipoComprobante)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TipoPago)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VentaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.VentaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VentaId })
        </td>
    </tr>
}

</table>
